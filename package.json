{
  "name": "pilotweave",
  "private": true,
  "version": "0.1.0",
  "description": "Local control plane for GitHub Copilot clients",
  "scripts": {
    "dev": "tauri dev --config apps/desktop/src-tauri/tauri.conf.json",
    "build": "tauri build --config apps/desktop/src-tauri/tauri.conf.json",
    "test": "node --check apps/desktop/web/app.js && cargo test --workspace",
    "check:web": "node --check apps/desktop/web/app.js",
    "check:rust": "cargo fmt --all -- --check && cargo test --workspace && cargo clippy --workspace --all-targets -- -D warnings",
    "check": "npm run check:web && npm run check:rust"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.8.0"
  }
}
